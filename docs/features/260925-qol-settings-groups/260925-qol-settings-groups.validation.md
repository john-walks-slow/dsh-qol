# qol-settings-groups 用户验证

## 验证说明

- 验证对象：设置 → QoL 面板按 4 组组织（导航与切换 / 输入与键盘 / 触摸与反馈 / 显示与设置页），组标题可点击折叠、状态持久化
- 环境/前置条件：手机访问 dsh Web（插件 dsh-qol 已加载，刷新页面生效）；设置页能正常打开
- 注意：改动仅在本地 lib 中，线上 4175 实例需刷新页面后生效（client bundle 按需读盘）

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| --- | --- | --- | --- | --- |
| 打开 设置 → QoL | 开关不再平铺一行到底：按 4 个分组小节呈现（导航与切换 / 输入与键盘 / 触摸与反馈 / 显示与设置页），每组标题下 2-5 个开关 | | 待验证 | |
| 检查各分组归属是否符合直觉 | 导航与切换：活跃会话 Tab Bar（含标签显示模式）/ 侧栏滑动开合 / 侧栏覆盖不挤宽 / 切换会话收起侧栏 / 切换会话不拉键盘；输入与键盘：输入法适配 / 隐藏权限下拉；触摸与反馈：按钮触摸反馈 / 禁用长按拖拽；显示与设置页：设置页全屏重写 / 记忆页签 / 代码块内滚 / 状态动画 | | 待验证 | |
| 点击「触摸与反馈」组标题 | 该组开关行收起隐藏，标题箭头方向变化；其他组不受影响 | | 待验证 | |
| 收起一组后刷新页面 | 该组保持收起状态（按浏览器持久化，键 dsh.qol.groups） | | 待验证 | |
| 再点标题展开后，开关仍可正常开关 | 展开后所有开关照常即时生效，全程无报错卡死 | | 待验证 | |
| 桌面端打开 设置 → QoL | 分组标题与折叠同样生效（不局限于移动端） | | 待验证 | |

## 验证结论

待验证。

## 待跟进

无。
