# tabbar-mode 用户验证

## 验证说明

- 验证对象：标签栏加号固定右侧 + 标签显示模式配置（标准 / 最近活跃）
- 环境/前置条件：手机访问 dsh Web（插件 dsh-qol ≥ 1.1.0 已加载）；有多个会话（含正在运行/等待处理/已完成未读的会话效果最直观）
- 注意：改动当前仅在本地 lib 中，线上 4175 实例需刷新页面后生效（client bundle 按需读盘）

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| --- | --- | --- | --- | --- |
| 打开会话较多的一屏，横向滑动标签栏 | 标签在滚动容器内滑动，最右侧"+"新建按钮始终固定可见，不随标签滚走 | | 待验证 | |
| 设置 → QoL → "活跃会话 Tab Bar"开关行下方出现"标签显示模式"分段选择器（仅开关开启时显示） | 显示"标准 / 最近活跃"两个选项，当前模式高亮，下方有对应说明文字 | | 待验证 | |
| 切换到"最近活跃"（不重启） | 立即生效：显示全部真实会话（不再只是打开过的），带状态点的会话（黄=待处理/绿=完成未读/蓝=运行中）排在前、灰色会话排后；各标签无"×"关闭按钮，中键点击也不关闭 | | 待验证 | |
| 保持"最近活跃"刷新页面 | 模式保持不变（按浏览器持久化） | | 待验证 | |
| 有会话在运行中/等待审批/刚完成时观察排序 | 黄/绿/蓝三种彩色状态按最近活跃时间混排在前（不按颜色分组），灰色空闲会话排后面；状态变化时排序自动更新 | | 待验证 | |
| 切回"标准" | 恢复现有行为：只显示打开过的会话、按打开顺序固定排列、有"×"可关闭 | | 待验证 | |
| 设置中把"活跃会话 Tab Bar"开关关闭再打开 | 关闭时顶部标签条立即消失（无空白残条）；重新打开立即恢复，全程无报错卡死 | | 待验证 | |

## 验证结论

待验证。

## 待跟进

无。
