# tabbar-mode 用户验证

## 验证说明

- 验证对象：标签栏加号固定右侧 + 标签显示模式配置（标准 / 最近活跃）
- 环境/前置条件：手机访问 dsh Web（插件 dsh-qol 已加载，含 2026-09-25 13:04 修订）；有多个会话（含正在运行/等待处理/已完成未读的会话效果最直观）
- 注意：改动当前仅在本地 lib 中，线上 4175 实例需刷新页面后生效

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| --- | --- | --- | --- | --- |
| 打开会话较多的一屏，横向滑动标签栏 | 标签在滚动容器内滑动，最右侧"+"新建按钮始终固定可见，不随标签滚走 | | 待验证 | |
| 设置 → QoL → "活跃会话 Tab Bar"开关行下方出现"标签显示模式"分段选择器（仅开关开启时显示） | 显示"标准 / 最近活跃"两个选项，当前模式高亮，下方有对应说明文字 | | 待验证 | |
| 先在"标准"下打开几个会话，再切换到"最近活跃"（不重启） | **同一组页签**：不增不减（不自动显示没打开过的会话），仅排序变化——带状态点的（黄=待处理/绿=完成未读/蓝=运行中）整体排前、区内最久未动的在最左；灰色空闲会话排后、区内最新在最左 | | 待验证 | |
| "最近活跃"下点某个标签的"×"或中键点击 | 页签关闭（与标准模式行为一致）；若关闭的是当前会话，自动落到**最近活跃**的那个剩余页签 | | 待验证 | |
| 在"最近活跃"关闭一个页签后切回"标准" | 关闭状态保留（同一套打开/关闭记录），不复活 | | 待验证 | |
| 保持"最近活跃"刷新页面 | 模式与已打开页签均保持（浏览器持久化） | | 待验证 | |
| 有会话在运行中/等待审批/刚完成时观察排序 | 带状态点的会话（黄/绿/蓝混排）整体排在前，区内**最久未动的在最左**（挂起最久的最先被看到）；灰色空闲会话排后，区内**最新在最左**；状态变化时排序自动更新 | | 待验证 | |
| 切回"标准" | 排序恢复按打开顺序固定排列，页签集合不变 | | 待验证 | |
| 设置中把"活跃会话 Tab Bar"开关关闭再打开 | 关闭时顶部标签条立即消失（无空白残条）；重新打开立即恢复，全程无报错卡死 | | 待验证 | |

## 验证结论

待验证。

## 待跟进

无。
